---
import BaseLayout from '@src/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import PostsByYear from '@src/components/PostsByYear.astro';

type BlogPost = CollectionEntry<'blog'>;

const lang = 'en';
const allPosts: BlogPost[] = (await getCollection('blog', ({ data }: any) => {
	return data.lang === 'en';
})).sort(
	(a: BlogPost, b: BlogPost) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<BaseLayout
	title='All Posts'
	description='All blog posts'
	lang={lang}
	alternateUrl="/posts/"
	alternateLang="es"
>
  <div class='container'>
    <div class="mb-10">
      <h1 class="text-3xl font-display font-bold uppercase tracking-tight mb-2">All Posts</h1>
      <p class="text-magi-muted text-lg">All my blog posts about technology, development and cloud.</p>
    </div>
    <PostsByYear posts={allPosts} lang={lang}/>
  </div>
</BaseLayout>
